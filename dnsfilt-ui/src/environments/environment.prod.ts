export const environment = {
  production: true,
  // When null in production, the UI automatically inherits the host and port of the running backend (e.g. 8080, 9090, 443)
  backendPort: null,
  // Custom API URL override if backend is hosted on a different domain/port
  apiUrl: ''
};
