export const environment = {
  production: false,
  // Configurable backend port for local development (e.g., 8080, 9090, 8081)
  backendPort: 9090,
  // Custom API URL override (optional, e.g. "http://localhost:9090/api")
  apiUrl: ''
};
